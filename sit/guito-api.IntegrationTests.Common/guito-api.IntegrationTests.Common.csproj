<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <!--
    Shared plumbing for the SIT suites (guito-api.IntegrationTests and
    guito-api-auth.IntegrationTests): live staging configuration and client
    factories. Not a test project — run-deployed-tests.sh never invokes it
    directly. The fixture reads env vars exported by scripts/run-deployed-tests.sh
    (base URL + agent keys from Secrets Manager); this process never touches AWS.
  -->

</Project>